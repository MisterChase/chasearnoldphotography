# Build output — regenerated by `npm run build`
dist/

# Your original high-res masters. These stay on your machine and are NEVER
# deployed. The build reads them and emits optimized copies into dist/.
source-media/

# Media optimization cache (safe to delete; just makes rebuilds faster)
.media-cache/

node_modules/
.DS_Store
