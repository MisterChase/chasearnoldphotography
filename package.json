{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal portfolio \u2014 static site generator with automatic media optimization.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "clean": "rm -rf dist .media-cache",
    "new": "node scripts/new-project.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
